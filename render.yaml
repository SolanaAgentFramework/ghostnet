services:
  - type: web
    name: dead-drop-backend
    env: node
    rootDir: pumpfun
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production

